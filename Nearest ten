#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

int n; 
scanf("%d",&n);
int r=n%10;
if (r>=5)
{
printf("%d\n",n+(10-r));
}
else 
{
 printf("%d\n",(n-r));
}
    return 0;
}
