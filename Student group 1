#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int p;
    scanf("%d",&p);
    int r=p/4;
    if(p>=100 && p<=200)
    {
        printf("%d\n%d\n%d\n%d\n",r,r,r,r+p%4);
    }
    else
    {
        printf("INVALID INPUT");
    }
    return 0;
}
