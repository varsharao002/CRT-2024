#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int d,m,y,n;
    scanf("%d%d%d%d",&d,&m,&y,&n);
    int sum=d+m+y;
    int product=3*4;
    if(sum%product==0&& n>=50 && n<=100)
    {
        printf("Cinema Day");
    }
    else
    {
        printf("Not a cinema Day");
    }
    return 0;
}
