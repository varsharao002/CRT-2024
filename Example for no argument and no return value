#include <stdio.h>
void reverse()
{
    int n,d,r=0;
    scanf("%d",&n);
    while(n>0)
    {
        d=n%10;
        n=n/10;
        r=r*10+d;
    }
    printf("%d",r);
}
        int main()
    {
        reverse();
return 0;
}
