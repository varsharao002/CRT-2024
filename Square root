#include <stdio.h>
#include <math.h>

int main() {
float a,b,c;
    scanf("%f%f%f",&a,&b,&c);
    float r1=(-b+sqrt(b*b-4*a*c)/2*a);
    float r2=(-b-sqrt(b*b-4*a*c)/2*a);
    printf("root1=%f\n",r1);
    printf("root2=%f\n",r2);
    return 0;
}
