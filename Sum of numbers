#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n,s=0;
    while(1)
    {
        scanf("%d",&n);
        if(n==-1){
            break;
    }
        s=s+n;
    }
    printf("%d",s);
    
    return 0;
}
