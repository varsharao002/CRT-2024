#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n,d,s=1;
    scanf("%d",&n);
    while(n>0)
    {
        d=n%10;
        if(d!=0)
        {
            s=s*d;
        }
        n=n/10;
    }
    printf("%d" ,s);


    return 0;
}
