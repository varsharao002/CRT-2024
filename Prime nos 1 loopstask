#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int i,j,m,n,c=0;
    scanf("%d",&m);
    scanf("%d",&n);
    for(i=m;i<=n;i++)
    {
        c=0;
            for(j=1;j<=i;j++)
        {
        if(i%j==0)
            c++;
    }
     
    if(c==2)
    printf("%d ",i);
         }
    return 0;
}
