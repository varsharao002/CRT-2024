#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n,d,s1=0;
    scanf("%d",&n);
    while(n>0)
    {
        d=n%10;
        n=n/10;
        if(d%2==1)
            s1=s1+d;
    }
    printf("%d",s1);
    return 0;
}
